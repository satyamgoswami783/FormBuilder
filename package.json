{
  "name": "custom-form-builder",
  "version": "1.0.0",
  "description": "A custom form builder application using MERN stack and Bootstrap.",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon backend/server.js",
    "build": " npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.21.2",
    "mongoose": "^5.13.23"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
